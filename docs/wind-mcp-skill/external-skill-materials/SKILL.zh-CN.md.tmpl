---
name: "{skill-name}"
description: >-
  {一句话说明这个 skill 能完成什么。} 当用户需要 {明确触发场景、用户说法、
  文件类型、工具集成或任务上下文} 时使用。不要用于 {相邻但不属于本 skill 的场景}。

# 可选增强字段：仅当目标运行时支持时保留。
# version: "{版本号}"
# author: "{作者或组织}"
# homepage: "{主页}"
# auto_invoke: true
# allowed-tools:
#   - Bash
#   - Read
#   - Edit
#   - AskUserQuestion
# triggers:
#   - "{用户可能说法 1}"
#   - "{用户可能说法 2}"
---

# {Skill 标题}

使用本 skill 完成 {核心目标}。流程必须聚焦在 {核心结果}，不要扩展到
{容易跑偏的相邻任务}。

<!--
写作约束：
1. description 是触发入口，必须写清楚“做什么 + 何时使用 + 何时不用”。
2. 正文只保留执行时必须进入上下文的流程、约束、决策和资源导航。
3. 大表、长示例、字段清单、错误码、模板正文优先迁移到 references/。
4. 可重复、脆弱、需要确定性的动作优先沉淀到 scripts/。
5. 输出要复用的图片、脚手架、文档样例优先放到 assets/。
6. 删除不适用章节；不要为了“完整”保留空章节。
-->

## 角色与目标

你在本 skill 中扮演 {角色，例如：金融数据路由器 / 设计合伙人 / 部署执行器}。

目标：

- {目标 1：用户最终应该得到什么}
- {目标 2：结果必须满足什么质量标准}
- {目标 3：需要怎样的验证或交接}

非目标：

- {明确不做的事 1}
- {明确不做的事 2}

## 适用范围

### 支持

- {支持能力 1}
- {支持能力 2}
- {支持能力 3}

### 不支持

- {排除范围 1}
- {排除范围 2}
- {排除范围 3}

### 数据、权限或环境前提

- {必需凭证、配置、文件、网络或工具}
- {时效、权限、额度、版本或平台限制}
- {不能通过错误 fallback 绕过的限制}

## 启动前置

<!-- 只有 skill 每次启动都必须执行检查、同步、更新或加载上下文时保留本节。 -->

在进入主流程前：

1. 运行 `{启动检查脚本或命令}`，用于 {更新检查 / 环境探测 / 状态加载}。
2. 读取 {必须读取的上下文文件或状态源}。
3. 如果输出包含 `{升级 / 配置 / 权限提示}`，按 {处理规则} 处理后继续。
4. 如果缺少必需工具或权限，停止并报告 `{阻塞状态}`。

不得把启动前置中的失败误判为业务失败。

## 硬门禁

- 在 {高风险动作、外部调用、写文件、打开网页、提交变更或阶段切换} 之前，必须先 {用户确认 / 参数校验 / 权限校验 / 环境校验}。
- 如果 {阻塞条件}，立即停止并说明缺少什么，不要用相邻路径绕过。
- 未经用户确认，不要执行 {需要用户参与或有副作用的动作}。
- 不要 {本 skill 最容易误用的行为}。

## 输入与上下文

只收集执行所需的最小上下文：

1. 读取 {用户请求、项目文件、配置、工件或引用资料}。
2. 判定 {任务类型、模式、标的、范围、权限、风险等级}。
3. 只有当本地上下文无法发现且误判代价高时，才向用户提问。
4. 每次只问一个最小问题；能给选项时优先给选项。

## 模式分流

<!-- 如果 skill 有多条主路径，保留本节；否则删除。 -->

| 条件 | 模式 | 后续流程 |
| --- | --- | --- |
| {条件 A} | {模式 A} | 进入 {阶段 / 工具 / 资源} |
| {条件 B} | {模式 B} | 进入 {阶段 / 工具 / 资源} |
| {条件 C} | {模式 C} | 停止 / 提问 / 兜底 |

如果用户请求同时命中多个模式，先选择 {主模式判定规则}，再决定是否拆分任务。

## 工作流

按顺序执行以下阶段。

### 阶段 1：建立上下文

- 读取 {必要文件 / 状态 / 用户输入}。
- 总结当前理解：{要确认的对象、范围和成功标准}。
- 如果范围过大，先帮助用户拆分，不要直接进入执行。

### 阶段 2：选择路径

- 如果 {模式 A 条件}，执行 {模式 A 流程}。
- 如果 {模式 B 条件}，执行 {模式 B 流程}。
- 如果输入不完整但可安全推断，采用 {保守默认值}。
- 如果输入不完整且不可安全推断，向用户提出一个最小问题。

### 阶段 3：执行

- 调用 {脚本、工具、API 或编辑流程}。
- 使用 {引用文件 / manifest / schema} 校验参数或结构。
- 对有副作用的动作，执行前再次确认是否已通过硬门禁。

### 阶段 4：验证

- 检查 {成功标准 1}。
- 检查 {成功标准 2}。
- 检查 {错误、遗漏、回归、格式、权限或数据口径}。

### 阶段 5：交付

- 返回 {主结果}。
- 附上 {证据、来源、测试、限制、失败原因或下一步}。
- 如果需要交接到下一个 skill、工具或人工步骤，按“交接”章节执行。

## 交互规则

<!-- 如果 skill 需要用户选择、审批或确认，保留本节。 -->

需要用户参与时，优先使用 AskUserQuestion。问题必须包含：

- 一个明确标题。
- 当前任务上下文。
- 为什么现在需要用户选择。
- 推荐选项及原因。
- 2 到 4 个互斥选项。

不得把需要选择的问题藏在长段落里。不得在用户尚未选择时继续执行会改变状态的动作。

如果当前运行时没有 AskUserQuestion 工具，就用相同选项向用户提问并等待回答。

## 决策规则

| 场景 | 动作 |
| --- | --- |
| {命中主路径} | 使用 {工具、流程或资源} |
| {输入不完整但可推断} | 采用 {保守默认值}，并在结果中说明 |
| {输入不完整且不可安全推断} | 问一个最小问题 |
| {用户要求跳过必要步骤} | 解释该步骤保护什么；若仍坚持，按 {紧急出口规则} |
| {工具失败但可本地修复} | 修复同一路径后重试 |
| {工具失败且不是本地可修复} | 停止并报告失败原因和下一步 |
| {请求超出范围} | 说明边界，必要时推荐其它 skill 或人工路径 |

如果本 skill 同时存在专用路径和通用兜底路径，专用路径能覆盖的部分必须先使用专用路径。
只有专用路径契约无法覆盖的剩余数据或动作，才使用通用兜底路径，并将结果合并。
不得仅因为请求复杂、多对象、多字段、带日期区间、要求表格化输出或使用自然语言表达，
就使用通用兜底路径。

## 工具与资源

本 skill 的资源按需加载，不要一次性读取所有 reference。

- 运行 `scripts/{script}`：当 {需要确定性执行、重复操作、复杂校验或封装外部调用}。
- 读取 `references/{reference}.md`：当 {需要详细规则、字段表、错误码、示例或长流程}。
- 使用 `assets/{asset}`：当 {输出要复用模板、图片、样例或脚手架}。

资源导航：

| 需要 | 读取或运行 |
| --- | --- |
| {工具合法性 / manifest} | `references/{manifest}` |
| {详细参数 / schema} | `references/{contract}` |
| {错误处理 / fallback} | `references/{errors}` |
| {shell / 平台差异} | `references/{platform}` |
| {稳定执行} | `scripts/{script}` |

## 输出契约

输出必须包含：

- {主输出，例如补丁、设计文档、查询结果、报告或配置结果}
- {验证信息，例如测试、来源、工具输出摘要或校验结果}
- {必要限制，例如未验证项、口径限制、权限限制或下一步}

当一致性重要时，使用以下形状：

```markdown
## 结果
{完成了什么}

## 证据
{验证、关键数据、命令、来源或工件路径}

## 风险
{剩余限制、未验证点、用户需要知道的影响}
```

## 工件模板

<!-- 只有 skill 必须写入设计文档、计划、报告或其它工件时保留本节。 -->

将 `{artifact-path}` 写成以下结构：

```markdown
# {工件标题}

## 背景
{用户目标、上下文和约束}

## 结论
{推荐方案或最终结果}

## 方案
{考虑过的路径、权衡和选择理由}

## 执行细节
{实现、调用、配置或操作步骤}

## 验证
{如何确认结果正确}

## 未决问题
{仍需用户、外部系统或后续任务确认的点}
```

## 校验清单

结束前检查：

1. 是否使用了所有关键输入。
2. 是否遵守硬门禁和权限边界。
3. 是否按正确模式、工具和资源执行。
4. 是否完成成功标准的验证。
5. 是否报告未执行、无法验证或失败的项。
6. 是否留下了用户能继续推进的下一步。

## 失败处理

- 如果 {常见错误 1}，执行 {处理动作}。
- 如果 {常见错误 2}，执行 {处理动作}。
- 如果 {权限、凭证、额度、网络或后端错误}，不得切换到不等价路径绕过。
- 如果所有允许路径失败，停止并说明：
  - 已尝试什么
  - 什么失败了
  - 为什么不能继续
  - 下一步需要什么

## 兜底与交接

<!-- 只有存在 fallback、下游 skill 或人工交接时保留本节。 -->

只有当 {主路径失败条件} 且失败原因属于 {允许兜底的类别} 时，才进入兜底。

不得因 {不允许兜底的错误，例如凭证、权限、额度、转义、网络} 切换路径。

进入兜底或交接前：

1. 说明已尝试的主路径。
2. 说明为什么当前条件满足兜底。
3. 询问用户是否切换到 {下游 skill / 人工步骤 / 替代工具}。
4. 用户确认后，按 {交接资源或流程} 执行。

## 状态记录

<!-- 只有 skill 需要跨会话记忆、分析、学习或审计时保留本节。 -->

当本次任务产生可复用经验时，记录到 `{状态文件或日志}`：

- `type`: `{pattern | pitfall | preference | architecture | tool | operational}`
- `insight`: `{非显而易见、未来能节省时间的发现}`
- `confidence`: `{1-10}`
- `source`: `{observed | user-stated | inferred}`
- `files`: `{相关文件路径}`

只记录真正有复用价值的发现，不记录显而易见的过程日志。

## 完成状态

最终用以下状态之一收束：

- `DONE`：目标完成且已验证。
- `DONE_WITH_CONCERNS`：目标完成，但存在已说明的限制或未验证项。
- `NEEDS_CONTEXT`：缺少必要上下文，无法安全继续。
- `BLOCKED`：受权限、凭证、工具、网络、用户确认或外部系统阻塞。
- `OUT_OF_SCOPE`：请求不属于本 skill 范围。

## 重要规则

- 先 {路由 / 理解 / 校验}，再 {执行 / 写入 / 调用外部系统}。
- 保持 {本 skill 的最高优先原则}。
- 不要 {最常见误用 1}。
- 不要 {最常见误用 2}。
- 对用户产生影响的限制必须明说。
